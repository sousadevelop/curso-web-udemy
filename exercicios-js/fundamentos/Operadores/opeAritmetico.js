const [a, b, c, d] = [5, 9, 7, 2];

const soma = a + b + c + d;
const subtracao = a - c;
const multiplicacao = b * d;
const divisao = b / a;
const modulo = a % 2;

console.log(soma, subtracao, multiplicacao, divisao, modulo);